{
  "// generated": "cross-skills {{GENERATED_DATE}} — edit uir.json and regenerate to update",

  "permissions": {
    "allow": [
      {{PERMISSIONS_ALLOW}}
    ],
    "deny": [
      {{PERMISSIONS_DENY}}
    ]
  },

  "hooks": [
    {{HOOKS_JSON}}
  ],

  "env": {
    {{ENV_VARS_JSON}}
  }
}
